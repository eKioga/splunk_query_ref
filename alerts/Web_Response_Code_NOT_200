```the query below is designed to work with the Web Monitoring Splunk app.```
```Because the weren't generated by an agent, The REGEX code below is needed
```to define aspects of the log for splunk to reconize and alert on```
sourcetype=web_ping NOT (response_code="" OR response_code=200)
| rex field=_raw "\"title\":\"(?<title>[^\"]+)\"" 
| rex field=_raw "\"url\":\"(?<url>[^\"]+)\""
| rex field=_raw "\"response_code\":\"(?<response_code>[^\"]+)\""
| table title, url, response_code
